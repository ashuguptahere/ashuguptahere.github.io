[project]
name = "resume"
version = "0.1.0"
description = "Resume for ashuguptahere.github.io"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
    "ruff>=0.14.10",
]
